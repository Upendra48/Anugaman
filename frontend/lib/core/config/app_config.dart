import 'package:flutter/material.dart';

// This class contains the configuration for the application, such as the base URL for the API.
class AppConfig{
  static const String baseUrl = "http://localhost:8000/api";
}